diff --git a/lib/index.cjs b/lib/index.cjs
index f4c8617a0f355d29ee97f45473c38cfe2d32f4c4..a0ea3aca1131a9916ca0a276587766ed0c91b71c 100644
--- a/lib/index.cjs
+++ b/lib/index.cjs
@@ -9,6 +9,17 @@ let vite = require("vite");
 let node_crypto = require("node:crypto");
 let url = require("url");
 let es_module_lexer = require("es-module-lexer");
+//#region src/utils/buildPaths.ts
+function rebaseImport(importSrc, dir) {
+	if (!dir) return importSrc;
+	const prefix = "./" + dir;
+	if (importSrc.startsWith(prefix)) return "./" + importSrc.slice(prefix.length);
+	const absPrefix = "/" + dir;
+	if (importSrc.startsWith(absPrefix)) return "/" + importSrc.slice(absPrefix.length);
+	if (importSrc.startsWith(dir)) return "./" + importSrc.slice(dir.length);
+	return importSrc;
+}
+//#endregion
 //#region src/utils/codeRewriter.ts
 const BASE64_CHARS = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
 var CodeRewriter = class {
@@ -1847,6 +1858,7 @@ const addEntry = ({ entryName, entryPath, fileName, inject = "entry", forceClien
 	let clientInjected = forceClientInjected ?? false;
 	let emittedFileName;
 	let skipTransformIds = /* @__PURE__ */ new Set();
+	let bootstrapDir = "";
 	function skipSvelteKitSsrBuild() {
 		return (_command === "build" || viteConfig?.command === "build") && viteConfig?.build?.ssr && require_packageUtils.hasPackageDependency("@sveltejs/kit");
 	}
@@ -2100,6 +2112,8 @@ const addEntry = ({ entryName, entryPath, fileName, inject = "entry", forceClien
 			if (htmlFileNames.length === 0) return;
 			const file = this.getFileName(emitFileId);
 			emittedFileName = file;
+			const lastSlash = file.lastIndexOf("/");
+			bootstrapDir = lastSlash !== -1 ? file.slice(0, lastSlash + 1) : "";
 			const resolvePath = (htmlFileName) => {
 				if (!viteConfig.experimental?.renderBuiltUrl) return viteConfig.base + file;
 				const result = viteConfig.experimental.renderBuiltUrl(file, {
@@ -2128,9 +2142,9 @@ const addEntry = ({ entryName, entryPath, fileName, inject = "entry", forceClien
 				let rewritten = false;
 				htmlContent = htmlContent.replace(scriptRegex, (scriptTag, entrySrc) => {
 					rewritten = true;
-					const bootstrapSource = getSystemBootstrapSource(initPath, entrySrc);
+					const bootstrapSource = getSystemBootstrapSource(bootstrapDir ? rebaseImport(initPath, bootstrapDir) : initPath, bootstrapDir ? rebaseImport(entrySrc, bootstrapDir) : entrySrc);
 					const bootstrapHash = (0, node_crypto.createHash)("sha256").update(bootstrapSource).digest("hex").slice(0, 8);
-					const bootstrapFileName = `mf-entry-bootstrap-${bootstrapIndex++}-${bootstrapHash}.js`;
+					const bootstrapFileName = `${bootstrapDir}mf-entry-bootstrap-${bootstrapIndex++}-${bootstrapHash}.js`;
 					const bootstrapRef = this.emitFile({
 						type: "asset",
 						fileName: bootstrapFileName,
diff --git a/lib/index.mjs b/lib/index.mjs
index dd80177c938763613658169528c165e9cae9ad5d..0624b796ae712bdef9a9ce2c625e4400022dcd63 100644
--- a/lib/index.mjs
+++ b/lib/index.mjs
@@ -8,6 +8,17 @@ import { version } from "vite";
 import { createHash } from "node:crypto";
 import { fileURLToPath } from "url";
 import { init, parse } from "es-module-lexer";
+//#region src/utils/buildPaths.ts
+function rebaseImport(importSrc, dir) {
+	if (!dir) return importSrc;
+	const prefix = "./" + dir;
+	if (importSrc.startsWith(prefix)) return "./" + importSrc.slice(prefix.length);
+	const absPrefix = "/" + dir;
+	if (importSrc.startsWith(absPrefix)) return "/" + importSrc.slice(absPrefix.length);
+	if (importSrc.startsWith(dir)) return "./" + importSrc.slice(dir.length);
+	return importSrc;
+}
+//#endregion
 //#region src/utils/codeRewriter.ts
 const BASE64_CHARS = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
 var CodeRewriter = class {
@@ -1846,6 +1857,7 @@ const addEntry = ({ entryName, entryPath, fileName, inject = "entry", forceClien
 	let clientInjected = forceClientInjected ?? false;
 	let emittedFileName;
 	let skipTransformIds = /* @__PURE__ */ new Set();
+	let bootstrapDir = "";
 	function skipSvelteKitSsrBuild() {
 		return (_command === "build" || viteConfig?.command === "build") && viteConfig?.build?.ssr && hasPackageDependency("@sveltejs/kit");
 	}
@@ -2099,6 +2111,8 @@ const addEntry = ({ entryName, entryPath, fileName, inject = "entry", forceClien
 			if (htmlFileNames.length === 0) return;
 			const file = this.getFileName(emitFileId);
 			emittedFileName = file;
+			const lastSlash = file.lastIndexOf("/");
+			bootstrapDir = lastSlash !== -1 ? file.slice(0, lastSlash + 1) : "";
 			const resolvePath = (htmlFileName) => {
 				if (!viteConfig.experimental?.renderBuiltUrl) return viteConfig.base + file;
 				const result = viteConfig.experimental.renderBuiltUrl(file, {
@@ -2127,9 +2141,9 @@ const addEntry = ({ entryName, entryPath, fileName, inject = "entry", forceClien
 				let rewritten = false;
 				htmlContent = htmlContent.replace(scriptRegex, (scriptTag, entrySrc) => {
 					rewritten = true;
-					const bootstrapSource = getSystemBootstrapSource(initPath, entrySrc);
+					const bootstrapSource = getSystemBootstrapSource(bootstrapDir ? rebaseImport(initPath, bootstrapDir) : initPath, bootstrapDir ? rebaseImport(entrySrc, bootstrapDir) : entrySrc);
 					const bootstrapHash = createHash("sha256").update(bootstrapSource).digest("hex").slice(0, 8);
-					const bootstrapFileName = `mf-entry-bootstrap-${bootstrapIndex++}-${bootstrapHash}.js`;
+					const bootstrapFileName = `${bootstrapDir}mf-entry-bootstrap-${bootstrapIndex++}-${bootstrapHash}.js`;
 					const bootstrapRef = this.emitFile({
 						type: "asset",
 						fileName: bootstrapFileName,
