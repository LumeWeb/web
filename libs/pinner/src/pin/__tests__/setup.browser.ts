/**
 * Pin module browser test setup - extends parent browser setup
 * Pin tests primarily use shared setup without additional configuration
 */

// Import parent browser setup
import "../../__tests__/setup.browser";
